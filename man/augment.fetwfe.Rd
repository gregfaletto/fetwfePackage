% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom_methods.R
\name{augment.fetwfe}
\alias{augment.fetwfe}
\title{Augment user-supplied data with fitted values and residuals from a fetwfe fit}
\usage{
\method{augment}{fetwfe}(x, data, ...)
}
\arguments{
\item{x}{An object of class \code{"fetwfe"}.}

\item{data}{A panel \code{data.frame} with one row per unit-period (any
sort order --- augment auto-sorts), containing the response column
under the same name used at fit time (see \code{x$response_col_name}).
First-period-treated units, if present, are auto-trimmed.}

\item{...}{Unused.}
}
\value{
A copy of \code{data} with two extra numeric columns: \code{.fitted}
and \code{.resid}.
}
\description{
Computes \code{.fitted = X \%*\% beta_hat + x$y_mean} and
\code{.resid = data[[x$response_col_name]] - .fitted}, then column-binds those
two columns onto \code{data}. The response mean and column name are stored on
the fitted object during fitting (the estimator internally centers \code{y}
before solving), so fitted values come back on the original-response
scale without the caller having to remember either.
}
\details{
\code{data} is auto-handled to match the fitted design: rows are auto-sorted
by \verb{(unit, time)}, and any first-period-treated units (whose treatment
effect cannot be identified by the estimator) are auto-trimmed via
\code{idCohorts()}. So you can pass the same raw \code{pdata} you handed to
\code{fetwfe()} --- the method takes care of alignment. The only hard
requirement is that \code{data} contains the response column under its
original name.
}
\examples{
\dontrun{
  sim <- simulateData(genCoefs(G = 3, T = 6, d = 2, density = 0.5,
                               eff_size = 2),
                      N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5)
  res <- fetwfeWithSimulatedData(sim)
  broom::augment(res, data = sim$pdata)
}
}
