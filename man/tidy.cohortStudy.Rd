% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom_methods.R
\name{tidy.cohortStudy}
\alias{tidy.cohortStudy}
\title{Tidy a \code{cohortStudy} object}
\usage{
\method{tidy}{cohortStudy}(x, ...)
}
\arguments{
\item{x}{A \code{cohortStudy} object returned by \code{\link[=cohortStudy]{cohortStudy()}}.}

\item{...}{Unused; present for S3 compatibility.}
}
\value{
A data frame with one row per treated cohort and columns
\code{term}, \code{estimate}, \code{std.error}, \code{statistic}, \code{p.value}, \code{conf.low},
\code{conf.high}, and (if present in the input) \code{selected}.
}
\description{
Returns a \code{broom}-style tidy data frame for the output of
\code{\link[=cohortStudy]{cohortStudy()}}. Renames the snake_case columns of \code{catt_df} to broom
conventions (\code{se} -> \code{std.error}, \code{p_value} -> \code{p.value},
\code{ci_low} / \code{ci_high} -> \code{conf.low} / \code{conf.high}) and adds a \code{term}
column (\code{"cohort_<cohort label>"}) plus a \code{statistic} column
(\code{estimate / std.error}) so the schema matches \code{tidy.eventStudy()}
for downstream \code{bind_rows()} consumers. When the input carries a
\code{selected} column (\code{fetwfe} / \code{betwfe}), it is passed through as the
final column.
}
\details{
Confidence intervals come from the cohort fit's stored bounds (which
encode the alpha passed at fit time); unlike \code{\link[=tidy.eventStudy]{tidy.eventStudy()}}, this
method does not recompute the CIs at a custom \code{conf.level} because the
standard errors in \code{catt_df} are already paired with the fit-time
bounds (\code{ci_low} / \code{ci_high}), so re-emitting those is the
minimum-surprise behavior.
}
\examples{
\dontrun{
  res <- fetwfeWithSimulatedData(
    simulateData(genCoefs(G = 3, T = 6, d = 2, density = 0.5, eff_size = 2),
                 N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5, seed = 123)
  )
  broom::tidy(cohortStudy(res))
}
}
