% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did_funcs.R
\name{getTreatInds}
\alias{getTreatInds}
\title{Get Indices of Base Treatment Effect Parameters}
\usage{
getTreatInds(R, T, d, num_treats)
}
\arguments{
\item{R}{Integer; the number of treated cohorts.}

\item{T}{Integer; the number of time periods.}

\item{d}{Integer; the number of time-invariant covariates.}

\item{num_treats}{Integer; the total number of base treatment effect
parameters, typically calculated by \code{getNumTreats(R, T)}.}
}
\value{
An integer vector containing the indices for the \code{num_treats} base
treatment effect parameters.
}
\description{
Determines the column indices in the full design matrix \code{X_ints} (or
corresponding coefficient vector \code{beta}) that correspond to the base
treatment effect parameters (\code{tau_rt}).
}
\details{
The full design matrix \code{X_ints} is structured with several blocks of
variables:
\enumerate{
\item Cohort fixed effects (\code{R} columns)
\item Time fixed effects (\code{T-1} columns)
\item Covariate main effects (\code{d} columns, if \code{d > 0})
\item Cohort-Covariate interactions (\code{d*R} columns, if \code{d > 0})
\item Time-Covariate interactions (\code{d*(T-1)} columns, if \code{d > 0})
The base treatment effects form the next block. This function calculates the
total number of columns in the preceding blocks (\code{base_cols}) and returns
\code{base_cols + 1} through \code{base_cols + num_treats}.
}
}
\keyword{internal}
