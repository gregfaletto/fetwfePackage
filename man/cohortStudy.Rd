% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_study.R
\name{cohortStudy}
\alias{cohortStudy}
\title{Per-cohort average treatment effects}
\usage{
cohortStudy(result)
}
\arguments{
\item{result}{A fitted object from \code{\link[=fetwfe]{fetwfe()}}, \code{\link[=etwfe]{etwfe()}}, \code{\link[=betwfe]{betwfe()}}, or
\code{\link[=twfeCovs]{twfeCovs()}} (or their \verb{*WithSimulatedData()} wrapper analogs, which
return the same classes).}
}
\value{
A data frame with class \code{c("cohortStudy", "catt_df", "data.frame")}
containing one row per treated cohort and columns:
\describe{
\item{cohort}{Character; the cohort label (the calendar time at which
the cohort first received treatment).}
\item{estimate}{Numeric; the per-cohort ATT estimate.}
\item{se}{Numeric; standard error for the per-cohort ATT (\code{NA} when
the Gram matrix is singular or, for \code{fetwfe()} / \code{betwfe()}, the
bridge penalty zeroed out the cohort).}
\item{ci_low, ci_high}{Numeric; the stored lower and upper
confidence-interval bounds, reflecting the fit's \code{ci_type} --
simultaneous (family-wise) by default, or pointwise \code{1 - alpha}
Wald bounds when the fit used \code{ci_type = "pointwise"} (\code{alpha} is
the value passed at fit time).}
\item{p_value}{Numeric; follows the fit's \code{ci_type}. Under
\code{"pointwise"}, the two-sided Wald p-value
(\verb{2 * pnorm(-|estimate / se|)}); under \code{"simultaneous"} (the
default), the single-step max-T multiplicity-adjusted (family-wise)
p-value matching the simultaneous band (#200). \code{NA} when \code{se} is \code{0}
or \code{NA}.}
\item{selected}{(\code{fetwfe()} / \code{betwfe()} only.) Logical; \code{TRUE} when
the bridge penalty left the cohort's ATT nonzero. Absent for
\code{etwfe()} and \code{twfeCovs()}, which do not perform selection.}
}
Use \code{tidy(cohortStudy(result))} (with the \code{broom} package loaded) to
reshape to broom convention (\code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{p.value}, \code{conf.low}, \code{conf.high}, optionally
\code{selected}); see \code{\link[=tidy.cohortStudy]{tidy.cohortStudy()}}.
}
\description{
Extracts per-cohort ATT estimates from a fitted FETWFE / ETWFE / BETWFE /
twfeCovs object as a tidy data frame. Parallel to \code{\link[=eventStudy]{eventStudy()}} for
event-time aggregation: the per-cohort information is already available
via \code{result$catt_df}, and \code{cohortStudy()} surfaces it through a
discoverable function with its own help page (so users can reach it via
\code{?cohortStudy} without having to know the slot name).

The function is a pass-through on \code{result$catt_df} modulo class: the
columns, their values, and their order are unchanged. The returned
object carries class \code{c("cohortStudy", "catt_df", "data.frame")}. The
\code{cohortStudy} class dispatches the broom tidier \code{\link[=tidy.cohortStudy]{tidy.cohortStudy()}};
the \code{catt_df} class preserves the helpful-error layer (introduced in
fetwfe 1.11.0) that intercepts pre-1.11.0 Title-Case column names
(\code{Cohort}, \verb{Estimated TE}, etc.) with a migration message; the
\code{data.frame} base preserves the standard data-frame methods (\code{print},
\code{head}, \code{nrow}, \code{dplyr::filter}, etc.).
}
\examples{
\dontrun{
  coefs <- genCoefs(G = 3, T = 6, d = 2, density = 0.5, eff_size = 2)
  dat <- simulateData(coefs, N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5)
  res <- fetwfeWithSimulatedData(dat)
  cs <- cohortStudy(res)
  cs
  # Broom-shape translation:
  if (requireNamespace("broom", quietly = TRUE)) {
    broom::tidy(cs)
  }
}
}
\seealso{
\code{\link[=eventStudy]{eventStudy()}} for the parallel event-time accessor;
\code{\link[=tidy.cohortStudy]{tidy.cohortStudy()}} for broom-shape translation.
}
