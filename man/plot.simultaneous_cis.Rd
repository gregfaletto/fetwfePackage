% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simultaneous_cis.R
\name{plot.simultaneous_cis}
\alias{plot.simultaneous_cis}
\title{Plot a simultaneous-confidence-interval object}
\usage{
\method{plot}{simultaneous_cis}(x, ...)
}
\arguments{
\item{x}{A \code{"simultaneous_cis"} object from \code{\link[=simultaneousCIs]{simultaneousCIs()}}.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the per-effect estimates with both bands overlaid -- the
wider simultaneous band (error bars) on top of the narrower pointwise band
(thick line range) -- for a \code{\link[=simultaneousCIs]{simultaneousCIs()}} result, so the family-wise
vs per-effect coverage trade-off is visible.
}
\note{
Requires the \code{ggplot2} package (in Suggests). Install via
\code{install.packages("ggplot2")} if it is not already installed. Matches
\code{R/plot.R}'s \code{.plot_estimator} precedent for ggplot2-dependent plot
methods.
}
