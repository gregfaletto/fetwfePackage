% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catt_df_class.R
\name{[<-.catt_df}
\alias{[<-.catt_df}
\title{Single-bracket assignment on a \code{catt_df} object}
\usage{
\method{[}{catt_df}(x, i, j, ...) <- value
}
\arguments{
\item{x}{A \code{catt_df} object.}

\item{i, j}{Row / column selectors; see \verb{[<-.data.frame}.}

\item{...}{Further arguments passed through.}

\item{value}{The value to assign.}
}
\value{
The modified \code{catt_df} object, as for \verb{[<-.data.frame}.
}
\description{
Intercepts column assignment by the pre-1.11.0 Title-Case names
and stops with a migration message. The check fires when an old
name appears in the column-selector position. Row-only assignment
(\code{df[i, ] <- v}) and assignment by new column names fall through
to the \code{data.frame} method via \code{NextMethod()}.
}
\details{
The \code{nargs()} distinction here mirrors the read-side \verb{[.catt_df},
but the threshold shifts by one because \verb{[<-} carries an extra
positional \code{value} argument: \code{df[i] <- v} has \code{nargs() == 3} and
i is the column selector; \code{df[i, j] <- v} and \code{df[i, ] <- v} have
\code{nargs() == 4} and j (if non-missing) is the column selector.
}
