% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom_methods.R
\name{tidy.cohortTimeATTs}
\alias{tidy.cohortTimeATTs}
\title{Tidy a \code{cohortTimeATTs} object}
\usage{
\method{tidy}{cohortTimeATTs}(x, ...)
}
\arguments{
\item{x}{A \code{cohortTimeATTs} object returned by \code{\link[=cohortTimeATTs]{cohortTimeATTs()}}.}

\item{...}{Unused; present for S3 compatibility.}
}
\value{
A data frame with one row per \verb{(cohort, time)} cell and columns
\code{term}, \code{time}, \code{estimate}, \code{std.error}, \code{statistic}, \code{p.value},
\code{conf.low}, \code{conf.high}, and (if present in the input) \code{selected}.
}
\description{
Returns a \code{broom}-style tidy data frame for the output of
\code{\link[=cohortTimeATTs]{cohortTimeATTs()}}. Renames the snake_case columns to broom conventions
(\code{se} -> \code{std.error}, \code{p_value} -> \code{p.value}, \code{ci_low} / \code{ci_high} ->
\code{conf.low} / \code{conf.high}), keeps the \code{time} column, and adds a \code{term}
column (\code{"cohort_<cohort label>_time_<time>"}) plus a \code{statistic} column
(\code{estimate / std.error}) so the schema parallels \code{\link[=tidy.cohortStudy]{tidy.cohortStudy()}} and
\code{\link[=tidy.eventStudy]{tidy.eventStudy()}} for downstream \code{bind_rows()} consumers. When the input
carries a \code{selected} column (\code{fetwfe} / \code{betwfe}), it is passed through as
the final column.
}
\details{
Confidence intervals are the pointwise \code{1 - alpha} Wald bounds
\code{\link[=cohortTimeATTs]{cohortTimeATTs()}} computed (encoding the \code{alpha} passed there); like
\code{\link[=tidy.cohortStudy]{tidy.cohortStudy()}} this method passes them through rather than
recomputing at a custom \code{conf.level}.
}
\examples{
\dontrun{
  res <- fetwfeWithSimulatedData(
    simulateData(genCoefs(G = 3, T = 6, d = 2, density = 0.5, eff_size = 2),
                 N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5, seed = 123)
  )
  broom::tidy(cohortTimeATTs(res))
}
}
