% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_study.R
\name{eventStudy}
\alias{eventStudy}
\title{Compute pooled event-time treatment-effect estimates}
\usage{
eventStudy(x, alpha = NULL, ci_type = NULL)
}
\arguments{
\item{x}{A fitted object of class \code{"fetwfe"}, \code{"etwfe"}, or \code{"betwfe"}.}

\item{alpha}{(Optional) Significance level for confidence intervals.
Defaults to \code{x$alpha} (the alpha used at fit time).}

\item{ci_type}{(Optional) Character; one of \code{"simultaneous"} or
\code{"pointwise"}, or \code{NULL} (default). Controls whether the returned
\code{ci_low} / \code{ci_high} are the event-study-family simultaneous (family-wise,
uniform) band or the per-event-time pointwise Wald intervals. \code{NULL}
inherits the fit's stored \code{ci_type} (so a fit made with the default
\code{ci_type = "simultaneous"} yields simultaneous event-study bands, which
\code{print} / \code{summary} / \code{plot} then display); for objects fitted before
version 1.16.0 (no \code{ci_type} slot) \code{NULL} falls back to \code{"pointwise"}. The
\code{se} column is identical under both settings; the interval bounds, their
critical-value multiplier, and the \code{p_value} differ (under
\code{"simultaneous"} the \code{p_value} is the single-step max-T multiplicity-
adjusted dual of the band, under \code{"pointwise"} the per-effect Wald
p-value; #200). Degenerate event times (no valid contributing cohorts)
carry \code{NA} bounds and \code{NA} \code{p_value} under both settings.}
}
\value{
A data frame with class \code{c("eventStudy", "data.frame")} and
columns:
\describe{
\item{event_time}{Integer; event time \code{e = t - r}, ranging from 0
to \code{T - 2}.}
\item{n_cohorts}{Integer; number of cohorts contributing to the
pooled estimate at event time \code{e}.}
\item{estimate}{Numeric; the pooled event-time ATT estimate.}
\item{se}{Numeric; combined standard error.}
\item{ci_low}{Numeric; lower bound of the (1 - alpha) Wald CI.}
\item{ci_high}{Numeric; upper bound of the (1 - alpha) Wald CI.}
\item{p_value}{Numeric; follows the fit's \code{ci_type}. Under
\code{"pointwise"}, the two-sided Wald p-value
(\verb{2 * pnorm(-|estimate / se|)}); under \code{"simultaneous"} (the
default), the single-step max-T multiplicity-adjusted (family-wise)
p-value matching the simultaneous band (#200). \code{NA} when \code{se} is \code{0}
or \code{NA}.}
}
Only post-treatment event times (\code{e >= 0}) are included; pre-treatment
placebo periods would require an extended regression specification and
are out of scope for this initial release.
}
\description{
For a fitted object from \code{fetwfe()}, \code{etwfe()}, or \code{betwfe()}, computes the
pooled event-time treatment-effect estimates \code{tau_E(e)}, defined as
cohort-weighted averages of the cell-level treatment-effect estimates at
each post-treatment event time \code{e = t - r} (where \code{t} is calendar time and
\code{r} is the cohort's first-treated calendar time). Weights are
sample-cohort-size weights (matching \code{did::aggte(type = "dynamic")}
convention).

Standard errors combine two terms, mirroring the package's existing
overall-ATT SE machinery: \code{var_1(e)} from regression-coefficient noise
(computed via the same \code{gram_inv} machinery the package uses for cohort
SEs, or the cluster-robust sandwich under \code{se_type = "cluster"}), and
\code{var_2(e)} from cohort-probability noise (analog of the existing
\code{getSecondVarTermOLS} / \code{getSecondVarTermDataApp} machinery, with the
multinomial Jacobian restricted to cohorts valid at event time \code{e}).
Combined as \code{sqrt(var_1 + var_2)} by default (asymptotically exact
under paper Theorem \code{te.asym.norm.thm}(\eqn{c'}) / Assumption (\eqn{\Psi}-IF), which
the package's default cohort-sample-proportions estimator satisfies);
the conservative Cauchy-Schwarz bound \verb{sqrt(var_1 + var_2 + 2 sqrt(var_1 * var_2))} is available via \code{se_type = "conservative"}
(for users with non-(\eqn{\Psi}-IF) propensity-score estimators). When
\code{indep_counts} was supplied at fit time, the tight formula applies
regardless of \code{se_type} (two-sample regime, Theorem (b)).
}
\examples{
\dontrun{
  coefs <- genCoefs(G = 3, T = 6, d = 2, density = 0.5, eff_size = 2)
  dat <- simulateData(coefs, N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5)
  res <- fetwfeWithSimulatedData(dat)
  eventStudy(res)
}
}
\seealso{
\code{\link[=cohortStudy]{cohortStudy()}} for the parallel per-cohort accessor.
}
