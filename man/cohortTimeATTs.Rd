% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_time_atts.R
\name{cohortTimeATTs}
\alias{cohortTimeATTs}
\title{Per-(cohort, time) average treatment effects}
\usage{
cohortTimeATTs(result, alpha = NULL)
}
\arguments{
\item{result}{A fitted object from \code{\link[=fetwfe]{fetwfe()}}, \code{\link[=etwfe]{etwfe()}}, or \code{\link[=betwfe]{betwfe()}} (or
their \verb{*WithSimulatedData()} wrapper analogs, which return the same
classes). \code{\link[=twfeCovs]{twfeCovs()}} objects are not supported (see Description).}

\item{alpha}{Numeric in \verb{(0, 1)}; the pointwise confidence level is
\code{1 - alpha}. Defaults to the \code{alpha} stored on the fit.}
}
\value{
A data frame with class \code{c("cohortTimeATTs", "data.frame")}
containing one row per \verb{(cohort, time)} treatment-effect cell, sorted by
cohort then time, with columns:
\describe{
\item{cohort}{Character; the cohort label (the calendar time at which the
cohort first received treatment), matching \code{\link[=cohortStudy]{cohortStudy()}}.}
\item{time}{Numeric; the calendar time of the cell, equal to the cohort's
adoption time plus the event time (\verb{0, 1, ...}). Real panels carry their
actual calendar times. For synthetic \code{genCoefs()} / \code{simulateData()}
fixtures (whose panel runs \verb{1, ..., T}, so the stored first year is \code{1})
this coincides with the 1-based panel-time index. (Only a hand-built or
legacy fit with no stored first year falls back to that panel-time index
directly.)}
\item{estimate}{Numeric; the cell's ATT estimate.}
\item{se}{Numeric; the pointwise standard error. \code{0} for a cell zeroed
out by the fusion/bridge penalty while other cells survive
(\code{fetwfe()} / \code{betwfe()}); \code{NA} when standard errors are unavailable ---
the fit was computed with \code{q >= 1}, the Gram matrix on the selected
support is singular, or the penalty zeroed the \emph{entire} treatment block
(no cells selected, so there is no support to recompute the Gram from;
this matches \code{\link[=eventStudy]{eventStudy()}}).}
\item{ci_low, ci_high}{Numeric; the pointwise \code{1 - alpha} Wald bounds
\verb{estimate -/+ qnorm(1 - alpha/2) * se}. \verb{(0, 0)} for a fused-away cell;
\code{NA} when \code{se} is \code{NA}.}
\item{p_value}{Numeric; the two-sided pointwise Wald p-value
\verb{2 * pnorm(-|estimate / se|)}. \code{NA} when \code{se} is \code{0} or \code{NA}.}
\item{selected}{(\code{fetwfe()} / \code{betwfe()} only.) Logical; \code{TRUE} when the
bridge penalty left the cell's estimate nonzero. Absent for \code{etwfe()},
which does not perform selection.}
}
Use \code{tidy(cohortTimeATTs(result))} (with the \code{broom} package loaded) to
reshape to broom convention; see \code{\link[=tidy.cohortTimeATTs]{tidy.cohortTimeATTs()}}.
}
\description{
Extracts the fully disaggregated treatment-effect estimates from a fitted
FETWFE / ETWFE / BETWFE object: one row for every \verb{(cohort, time)} cell, with
no averaging over cohorts or over event time. This is the finest-grained view
of the estimated effects --- the \code{num_treats} underlying parameters
themselves --- complementing \code{\link[=cohortStudy]{cohortStudy()}} (which averages each cohort's
cells over time) and \code{\link[=eventStudy]{eventStudy()}} (which averages over cohorts at each
event time).

Like \code{\link[=eventStudy]{eventStudy()}}, this accessor is not available for \code{\link[=twfeCovs]{twfeCovs()}} objects:
that estimator has a single treatment-effect parameter per cohort (no
per-time disaggregation), so its finest granularity is already
\code{\link[=cohortStudy]{cohortStudy()}}.

Standard errors are the per-cell regression standard errors
\eqn{\sqrt{\sigma_\varepsilon^2 \, \psi' G^{-1} \psi / (NT)}}, recomputed at
call time from the fit's stored design (the same Gram-matrix machinery
\code{\link[=eventStudy]{eventStudy()}} uses; nothing is added to the fitted object). Because each
cell is a single cohort-time parameter, the cohort-probability sampling
variance that contributes to the aggregated \code{\link[=cohortStudy]{cohortStudy()}} /
\code{\link[=eventStudy]{eventStudy()}} standard errors is identically zero here, so a cell's SE is a
single coefficient's regression SE.

Confidence intervals and p-values are \emph{pointwise} \eqn{1 - \alpha} Wald
quantities (\verb{estimate +/- z * se}). For simultaneous (family-wise) bands
over the cell family, use
\code{simultaneousCIs(result, family = "all_post_treatment")}.
}
\details{
The cell standard error is computed from \code{psi}, the cell's row of the
(selected) treatment-effect design --- for \code{fetwfe()} the relevant row of the
inverse fusion transform \eqn{D^{-1}} in the transformed (theta) coordinate
space, for \code{betwfe()} / \code{etwfe()} a unit selector in the original (beta)
coordinate space restricted to the selected support. It is
never gated on the point estimate: a cell whose estimate is exactly zero
because the penalty fused it away has an all-zero \code{psi} and therefore
\code{se = 0} (the correct degenerate value), while a cell whose estimate happens
to be near zero for other reasons still receives its proper nonzero SE.
}
\examples{
\dontrun{
  coefs <- genCoefs(G = 3, T = 6, d = 2, density = 0.5, eff_size = 2)
  dat <- simulateData(coefs, N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5, seed = 123)
  res <- fetwfeWithSimulatedData(dat)
  cta <- cohortTimeATTs(res)
  cta
  # Broom-shape translation:
  if (requireNamespace("broom", quietly = TRUE)) {
    broom::tidy(cta)
  }
}
}
\seealso{
\code{\link[=cohortStudy]{cohortStudy()}} for the per-cohort (time-averaged) accessor;
\code{\link[=eventStudy]{eventStudy()}} for the per-event-time (cohort-averaged) accessor;
\code{\link[=simultaneousCIs]{simultaneousCIs()}} for simultaneous (family-wise) bands over the cell
family (\code{family = "all_post_treatment"}); \code{\link[=tidy.cohortTimeATTs]{tidy.cohortTimeATTs()}} for
broom-shape translation.
}
