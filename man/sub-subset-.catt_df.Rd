% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catt_df_class.R
\name{[[<-.catt_df}
\alias{[[<-.catt_df}
\title{Double-bracket assignment on a \code{catt_df} object}
\usage{
\method{[[}{catt_df}(x, i, ...) <- value
}
\arguments{
\item{x}{A \code{catt_df} object.}

\item{i}{Index; passed through to \verb{[[<-.data.frame}. Character
indices matching an old column name are intercepted.}

\item{...}{Further arguments passed through.}

\item{value}{The value to assign.}
}
\value{
The modified \code{catt_df} object, as for \verb{[[<-.data.frame}.
}
\description{
Intercepts assignment by the pre-1.11.0 Title-Case column names
(e.g., \code{df[["Estimated TE"]] <- v}) and stops with a migration
message pointing to the new snake_case name. This closes the gap
where a partial migration (RHS updated to new name, LHS still old)
would silently append a new column rather than overwriting the
renamed one. All other assignment falls through to the
\code{data.frame} method via \code{NextMethod()}.
}
