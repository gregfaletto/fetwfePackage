% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom_methods.R
\name{tidy.betwfe}
\alias{tidy.betwfe}
\title{Tidy a \code{betwfe} fitted object}
\usage{
\method{tidy}{betwfe}(x, conf.int = TRUE, conf.level = 1 - x$alpha, ...)
}
\arguments{
\item{x}{An object of class \code{"betwfe"} returned by \code{\link[=betwfe]{betwfe()}}.}

\item{conf.int}{Logical; include CI columns.}

\item{conf.level}{Numeric in (0, 1); defaults to \code{1 - x$alpha}. Applies only
to the overall-ATT row; the cohort rows pass through the fit-time
\code{catt_df} bounds (reflecting the fit's \code{ci_type}) and are not recomputed
at \code{conf.level} (#197). See \code{\link[=tidy.fetwfe]{tidy.fetwfe()}}.}

\item{...}{Unused.}
}
\value{
A data frame with \code{G + 1} rows.
}
\description{
Like \code{\link[=tidy.fetwfe]{tidy.fetwfe()}} but for a BETWFE fit. Includes the \code{selected}
column reflecting BETWFE's bridge-penalized selection.
}
\examples{
\dontrun{
  res <- betwfeWithSimulatedData(
    simulateData(genCoefs(G = 3, T = 6, d = 2, density = 0.5, eff_size = 2),
                 N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5)
  )
  broom::tidy(res)
}
}
