% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.fetwfe}
\alias{plot.fetwfe}
\title{Plot CATT or event-study estimates from a fitted FETWFE / ETWFE / BETWFE}
\usage{
\method{plot}{fetwfe}(x, type = c("event_study", "catt"), conf_int = TRUE, alpha = NULL, ...)
}
\arguments{
\item{x}{A fitted object from \code{\link[=fetwfe]{fetwfe()}}, \code{\link[=etwfe]{etwfe()}}, or \code{\link[=betwfe]{betwfe()}}.
(\code{twfeCovs} is not currently supported -- see GitHub issue #58 for
the broader treatment of \code{twfeCovs} class methods.)}

\item{type}{Character; either \code{"event_study"} (default; event-time
pooled coefficients from \code{\link[=eventStudy]{eventStudy()}}) or \code{"catt"} (per-cohort
ATTs from \code{result$catt_df}).}

\item{conf_int}{Logical; if \code{TRUE} (default), include confidence
interval error bars.}

\item{alpha}{Numeric; overrides the fit's alpha for CI computation.
\code{NULL} (default) uses the fit's alpha (so 95\% CIs when the fit's
alpha is 0.05). For the \code{"event_study"} view, this is forwarded to
\code{\link[=eventStudy]{eventStudy()}}; for the \code{"catt"} view, this triggers a recomputation
of \code{ci_low} / \code{ci_high} from \verb{estimate +/- qnorm(1 - alpha/2) * se}.}

\item{...}{Currently unused; reserved for future arguments.}
}
\value{
A \code{ggplot} object. Users can customize further via standard
ggplot layer-addition syntax (e.g.,
\code{plot(res) + ggplot2::theme_classic()}).
}
\description{
Returns a ggplot object showing either event-study coefficients
(default; \code{type = "event_study"}) or per-cohort average treatment
effects (\code{type = "catt"}) from a fitted estimator. Mirrors the
visualization style of \code{did::ggdid()} from the Callaway-Sant'Anna
\code{did} package, providing a single-call route from a fitted object to
a publication-ready visualization.

For \code{fetwfe} / \code{betwfe} (the bridge-penalty estimators) in the CATT
view, points are shape- and color-coded by whether the bridge
penalty left that cohort's ATT nonzero (\code{selected = TRUE}) or
zeroed it out (\code{selected = FALSE}). For \code{etwfe} (no selection),
all points are uniformly styled.
}
\examples{
\dontrun{
  coefs <- genCoefs(R = 3, T = 6, d = 2, density = 0.5, eff_size = 2)
  dat <- simulateData(coefs, N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5)
  res <- fetwfeWithSimulatedData(dat)
  if (requireNamespace("ggplot2", quietly = TRUE)) {
    plot(res)                          # default: event-study coefficients
    plot(res, type = "catt")           # per-cohort ATTs
    plot(res, conf_int = FALSE)        # point estimates only
    plot(res, alpha = 0.1)             # 90\% CIs
  }
}

}
\seealso{
\code{\link[=cohortStudy]{cohortStudy()}} for the per-cohort accessor;
\code{\link[=eventStudy]{eventStudy()}} for the event-time accessor;
\code{\link[=plot.etwfe]{plot.etwfe()}}, \code{\link[=plot.betwfe]{plot.betwfe()}} for the parallel methods.
}
