% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom_methods.R
\name{tidy.twfeCovs}
\alias{tidy.twfeCovs}
\title{Tidy a \code{twfeCovs} fitted object}
\usage{
\method{tidy}{twfeCovs}(x, conf.int = TRUE, conf.level = 1 - x$alpha, ...)
}
\arguments{
\item{x}{An object of class \code{"twfeCovs"} returned by \code{\link[=twfeCovs]{twfeCovs()}}.}

\item{conf.int}{Logical; include CI columns.}

\item{conf.level}{Numeric in (0, 1); defaults to \code{1 - x$alpha}. Applies only
to the overall-ATT row; the cohort rows pass through the fit-time
\code{catt_df} bounds (reflecting the fit's \code{ci_type}) and are not recomputed
at \code{conf.level} (#197). See \code{\link[=tidy.fetwfe]{tidy.fetwfe()}}.}

\item{...}{Unused.}
}
\value{
A data frame with \code{G + 1} rows.
}
\description{
Like \code{\link[=tidy.etwfe]{tidy.etwfe()}} but for a TWFE-with-covariates fit. Has no \code{selected}
column (\code{twfeCovs} is pure OLS and does no regularized selection).
\code{twfeCovs} estimates one pooled effect per cohort, so the returned frame
has the same \code{G + 1} rows (overall ATT in row 1, then one row per cohort)
as the sibling estimators.
}
\examples{
\dontrun{
  res <- twfeCovsWithSimulatedData(
    simulateData(genCoefs(G = 3, T = 6, d = 2, density = 0.5, eff_size = 2),
                 N = 120, sig_eps_sq = 1, sig_eps_c_sq = 0.5, seed = 123)
  )
  broom::tidy(res)
}
}
